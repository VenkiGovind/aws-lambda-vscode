export interface BankInfo {
  accountName?: string
  accountNumber: number
  routingNumber: number
}

export interface customerValidationInfo {
  repId: string
  repCert: string
  serviceName: string
}